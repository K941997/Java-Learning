Array Copy Array:
- Thư viện Util.Arrays

- Cách 1: Copy Dùng phép gán "=" -> sẽ thay đổi cả mảng gốc
	+ 	int[] mang1a = mang1;
	
- Cách 2: Copy dùng hàm .clone() -> ko thay đổi mảng gốc
	+	int[] mang1b = mang1.clone();
	
- Cách 3: Copy dùng System.  -> ko thay đổi mảng gốc
	+ System.arraycopy(mang1, 0, mang1c, 0, mang1.length);
	
- Arrays.toString(): Để hiện mảng bằng String